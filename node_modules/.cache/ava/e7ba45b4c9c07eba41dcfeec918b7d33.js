'use strict';

var _ava = require('ava');

/*function uniq(array) {
    let seen = {}
    return array.reduce((result, current) => {
        if (current in seen) {
            return result
        }
        seen[current] = true
        return result.concat(current)
    }, [])
}*/

function uniq(arr) {
    let seen = {};
    return arr.reduce((result, current) => {
        if (!seen[current]) {
            seen[current] = true;
            result.push(current);
        }
        return result;
    }, []);
}

(0, _ava.test)(t => t.deepEqual(uniq([1, 4, 2, 2, 3, 4, 8]), [1, 4, 2, 3, 8]));
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/e7ba45b4c9c07eba41dcfeec918b7d33.js.map