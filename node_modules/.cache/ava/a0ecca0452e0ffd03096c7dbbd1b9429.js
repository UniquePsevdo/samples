'use strict';

var _ava = require('ava');

/*let fibonacci = function(){
    let cache =[0, 1];
    let fib = function(index){
        let result = cache[index]
        if(typeof result!=='number'){
            result = fib(index-1) + fib(index-2)
        }
        return result;
    }
    return fib;
}();*/

let fibonacci = function () {
    let cache = [0, 1];

    let fib = function (n) {
        let result = cache[n];
        if (typeof result !== 'number') {
            result = fib(n - 1) + fib(n - 2);
            cache[n] = result;
        }
        return result;
    };
}();

(0, _ava.test)(t => t.is(fibonacci(5), 5));
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/a0ecca0452e0ffd03096c7dbbd1b9429.js.map