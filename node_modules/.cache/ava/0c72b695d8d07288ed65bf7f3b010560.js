"use strict";

const arr = [8, 87, 3, 0, -12, 88, 4351, 0.7];

function bs7(arr, val) {
    let guesIdx,
        minIdx = 0,
        maxIdx = arr.length - 1;
    while (minIdx < maxIdx) {
        guesIdx = Math.floor((minIdx + maxIdx) / 2);
        if (arr[guesIdx] === val) {
            return guesIdx;
        } else if (arr[guesIdx] > arr[minIdx]) {
            minIdx = guesIdx + 1;
        } else {
            maxIdx = guesIdx - 1;
        }
    }
    return -1;
}

console.log(bs7(arr, 0));
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/0c72b695d8d07288ed65bf7f3b010560.js.map