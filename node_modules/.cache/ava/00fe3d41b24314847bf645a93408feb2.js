'use strict';

var _ava = require('ava');

function includes(array, number) {
    return binarySearch(Array.sort(array), number) > -1;
}

/*
function binarySearch(arr, val){
    let minIdx = 0,
        maxIdx = arr.length-1,
        guessIdx;
    while(minIdx<maxIdx){
        guessIdx = Math.floor((maxIdx+minIdx)/2)
        if(arr[guessIdx] === val){
            return guessIdx
        }else if(arr[minIdx]<arr[guessIdx]){
            minIdx = guessIdx + 1
        }else{
            maxIdx = guessIdx - 1
        }
    }
    return -1
}
*/

function binarySearch(arr, val) {
    let guess;
    let min = 0;
    let max = arr.length - 1;
    while (min < max) {
        guess = Math.floor((min + max) / 2);
        if (arr[guess] === val) {
            return guess;
        } else if (arr[guess] < val) {
            min = guess + 1;
        } else {
            max = guess - 1;
        }
    }
    return -1;
}

const arr = [8, 87, 3, 0, -12, 88, 4351, 0.7];

(0, _ava.test)(t => t.is(includes(arr, 0), true));
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/00fe3d41b24314847bf645a93408feb2.js.map