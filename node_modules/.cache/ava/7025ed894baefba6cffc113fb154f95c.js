'use strict';

var _ava = require('ava');

function sort(arr) {
    if (arr.length < 2) {
        return arr;
    }

    let mid = Math.floor(arr.length / 2);
    let left = arr.slice[(0, mid)],
        right = arr.slice(mid);
    return sort(merge(left, right));
}

function merge(left, right) {
    let leftIdx = 0,
        rightIdx = 0,
        result = [];
    while (leftIdx < left.length && rightIdx < right.length) {
        result.push(left[leftIdx] < right[rightIdx] ? left[leftIdx++] : right[rightIdx++]);
    }
    return result.concat(left.slice(leftIdx)).concat(right.slice(rightIdx));
}

(0, _ava.test)(t => t.deepEqual(sort([11, 12, 3, 9, 10, 5]), [3, 5, 9, 10, 11, 12]));
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/7025ed894baefba6cffc113fb154f95c.js.map