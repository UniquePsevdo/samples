'use strict';

var _ava = require('ava');

/*let fibonacci = function(){
    let cache =[0, 1];
    let fib = function(index){
        let result = cache[index]
        if(typeof result!=='number'){
            result = fib(index-1) + fib(index-2)
        }
        return result;
    }
    return fib;
}();*/

let fibonacci = function () {
    let cache = [0, 1];
    return function fib(val) {
        let result;
        if (typeof cache[val] === 'number') {
            result = cache[val];
        } else {
            result = fib(val - 1) + fib(val - 2);
            cache[val] = result;
        }
        return result;
    };
}();

(0, _ava.test)(t => t.is(fibonacci(5), 5));
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/22d8bf5126463fd0f80572dea88824c7.js.map