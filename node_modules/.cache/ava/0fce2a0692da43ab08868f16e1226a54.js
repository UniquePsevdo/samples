'use strict';

var _ava = require('ava');

let seq = async arr => {
    let result = [];
    for (let a of arr) {
        result.push((await a()));
    }
    return result;
};


(0, _ava.test)(async t => {
    let a = () => Promise.resolve('a');
    let b = () => Promise.resolve('b');
    let c = () => new Promise(resolve => setTimeout(() => {
        resolve('c');
    }));
    t.deepEqual(['a', 'b', 'c'], (await seq([a, b, c])));
});
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/0fce2a0692da43ab08868f16e1226a54.js.map