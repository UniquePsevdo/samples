'use strict';

var _ava = require('ava');

/*
function reduce(val, fn, arr){
    for(let i=0; i < arr.length; i++){
        let current = arr[i];
        val = fn(val, current, i, arr);
    }
    return val;
}*/
function reduce(arr, fn, val) {
    for (let i = 0; i < arr.length; i++) {
        val = fn(val, arr[i], i, arr);
    }
    return val;
}

function reducer(arr, fn, val) {
    for (let i = 0; i < arr.length; i++) {
        val = fn(val, arr[i]);
    }
    return val;
}

(0, _ava.test)(t => t.is(reduce([1, 2, 3, 4], (result, current) => {
    return result + current;
}, 0), 10));
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/026133fa762e6479c2b7a87a96ee8728.js.map