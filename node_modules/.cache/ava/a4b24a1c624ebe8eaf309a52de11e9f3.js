'use strict';

var _ava = require('ava');

/*let reduceAsync = async (array, fn, value) => {
    for (let a of array) {
        value = fn(value, await a())
    }
    return value
}*/

/*
let reduceAsync = async (array, fn, val)=>{
    for(let a of array){
        val = fn(val, await a())
    }
    return val
}
*/

let reduceAsync = async function (arr, fn, val) {
    for (let a of arr) {
        val = fn(val, (await a()));
    }
    return val;
};

(0, _ava.test)(t => {
    let a = () => Promise.resolve('a');
    let b = () => Promise.resolve('b');
    let c = () => new Promise(resolve => {
        setTimeout(() => {
            resolve('c');
        }, 100);
    });

    t.deepEqual(['a', 'b', 'c'], reduceAsync([a, b, c], (acc, current) => [...acc, current], []));
    t.deepEqual(['d', 'a', 'b', 'c'], reduceAsync([a, b, c], (acc, current) => [...acc, current], ['d']));
});
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/a4b24a1c624ebe8eaf309a52de11e9f3.js.map