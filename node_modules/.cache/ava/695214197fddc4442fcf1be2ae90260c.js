'use strict';

var _ava = require('ava');

function quickSort(arr) {
    if (arr.length < 2) {
        return arr;
    }
    let pivot = arr[arr.length - 1];
    let left = [];
    let right = [];
    for (let i = 0; i < arr.length; i++) {
        if (arr[i] < arr[pivot]) {
            left.push(arr[i]);
        } else {
            right.push(arr[i]);
        }
    }
    return [...quickSort(left), pivot, ...quickSort(right)];
}

const arr = [1, 45, 0, 4, 22, 11, 18];

(0, _ava.test)(t => {
    t.deepEqual(quickSort(arr), [0, 1, 4, 11, 18, 22, 45]);
});
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/695214197fddc4442fcf1be2ae90260c.js.map