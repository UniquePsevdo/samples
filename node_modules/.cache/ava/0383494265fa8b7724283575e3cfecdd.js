'use strict';

var _ava = require('ava');

function throttle(fn, delay = 100) {
    let timer = null;
    return (...args) => {
        if (!timer) {
            timer = setTimeout(() => {
                fn.call(this, args);
                timer = null;
            }, delay);
        }
    };
}

function sayHi(event) {
    console.log('Hi!', this, event.type);
}

// Start
const throttled = throttle(sayHi, 500);
//window.addEventListener('mousemove', throttled);

(0, _ava.test)(t => {
    t.is(1, 1);
});
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/0383494265fa8b7724283575e3cfecdd.js.map