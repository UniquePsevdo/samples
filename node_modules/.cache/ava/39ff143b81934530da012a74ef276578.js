'use strict';

var _ava = require('ava');

let seq = async arr => {
    let result = [];
    for (let a of arr) {
        result.push((await a()));
    }
    return result;
};

(0, _ava.test)(async t => {
    let a = () => new Promise(resolve => {
        setTimeout(() => {
            resolve('a');
        }, 500);
    });
    let b = () => new Promise(resolve => {
        setTimeout(() => {
            resolve('b');
        }, 500);
    });
    let c = () => new Promise(resolve => {
        setTimeout(() => {
            resolve('c');
        }, 500);
    });

    t.deepEqual((await seq([a, b, c])), ['a', 'b', 'c']);
});
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/39ff143b81934530da012a74ef276578.js.map