'use strict';

var _ava = require('ava');

/*
function uniq(arr){
    let seen = {}
    return arr.reduce((result, current)=>{
        if(!seen[current]){
            seen[current] = true
            result.push(current)
        }
        return result
    },[])
}
*/

function uniq(arr) {
    let seen = {};
    return arr.reduce((acc, current) => {
        if (!seen[current]) {
            acc.push(current);
            seen[current] = true;
        }
        return acc;
    });
}

(0, _ava.test)(t => t.deepEqual(uniq([1, 4, 2, 2, 3, 4, 8]), [1, 4, 2, 3, 8]));
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/dde641e7967fbdb0edb3bb05f303c223.js.map