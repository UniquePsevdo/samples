'use strict';

var _ava = require('ava');

/*let seq = async(arr)=>{
   let result = []
   for(let a of arr){
       result.push(await a())
   }
   return result
}*/
async function seq(arr) {
    let result = [];
    for (let a of arr) {
        result.push((await a()));
    }
    return result;
}


/*
test( async t =>{
    let a = () => Promise.resolve('a')
    let b = () => Promise.resolve('b')
    let c = () => new Promise(resolve => setTimeout(()=>{ resolve('c') }, 100));

    t.deepEqual(['a','b','c'], await seq([a, b, c]))
})*/

(0, _ava.test)(t => {
    let a = () => Promise.resolve('a');
    let b = () => Promise.resolve('b');
    let c = () => new Promise(resolve => setTimeout(() => {
        resolve('c');
    }));
    t.deepEqual(['a', 'b', 'c'], [a, b, c]);
});
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/18cbc82467502c7543570a8a360e08a3.js.map