'use strict';

var _ava = require('ava');

let seq = async arr => {
    let result = [];
    for (let a of arr) {
        result.push((await a()));
    }
    return result;
};


(0, _ava.test)(async t => {
    let a = () => Promise.resolve('a');
    let b = () => Promise.resolve('b');
    let c = () => new Promise(resolve => setTimeout(() => {
        resolve('c');
    }, 100));

    t.deepEqual(['a', 'b', 'c'], (await seq([a, b, c])));
});
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/513eebef7a9a42b1ef6eb65a81020e58.js.map