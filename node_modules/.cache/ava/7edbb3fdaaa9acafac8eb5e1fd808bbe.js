'use strict';

var _ava = require('ava');

function includes(array, number) {
    return binarySearch(array, number) > -1;
}

/*
function binarySearch(arr, val){
    let minIdx = 0,
        maxIdx = arr.length-1,
        guessIdx;
    while(minIdx<maxIdx){
        guessIdx = Math.floor((maxIdx+minIdx)/2)
        if(arr[guessIdx] === val){
            return guessIdx
        }else if(arr[minIdx]<arr[guessIdx]){
            minIdx = minIdx + 1
        }else{
            maxIdx = maxIdx - 1
        }
    }
    return -1
}
*/

function binarySearch(arr, val) {
    let minIdx = 0,
        maxIdx = arr.length - 1,
        guessIdx;
    while (minIdx < maxIdx) {
        guessIdx = Math.floor((minIdx + maxIdx) / 2);
        if (arr[guessIdx] === val) {
            return guessIdx;
        } else if (arr[minIdx] < arr[guessIdx]) {
            minIdx = guessIdx + 1;
        } else {
            maxIdx = guessIdx - 1;
        }
    }
    return -1;
}

const arr = [8, 87, 3, 0, -12, 88, 4351, 0.7];

(0, _ava.test)(t => t.is(binarySearch(arr, 0), 3));
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/7edbb3fdaaa9acafac8eb5e1fd808bbe.js.map