'use strict';

var _ava = require('ava');

/*let fibonacci = function(){
    let cache =[0, 1];
    let fib = function(index){
        let result = cache[index]
        if(typeof result!=='number'){
            result = fib(index-1) + fib(index-2)
        }
        return result;
    }
    return fib;
}();*/

let fibonacci = function () {
    let cache = [0, 1];

    let fib = function (idx) {
        let result = cache[idx];
        if (typeof cache[idx] !== 'number') {
            result = fib(idx - 1) + fib(idx - 2);
            cache[idx] = result;
        }
        return result;
    };
    return fib;
}();

(0, _ava.test)(t => t.is(fibonacci(5), 5));
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/26ff4465aa303ac1c496f571a45c0d75.js.map