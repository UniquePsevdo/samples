'use strict';

var _ava = require('ava');

var _ava2 = _interopRequireDefault(_ava);

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/*
class LinkedList {
    constructor(head, ...tail) {
        this.head = head
        this.tail = tail.length
            ? new LinkedList(...tail)
            : null
    }
    add(item) {
        if (this.tail) {
            this.tail.add(item)
        } else {
            this.tail = new LinkedList(item)
        }
    }
    has(item) {
        if (this.head === item) {
            return true
        }
        if (this.tail === null) {
            return false
        }
        return this.tail.has(item)
    }
}
*/

class LinkedList {
    constructor(head, ...tail) {
        this.head = head;
        this.tail = tail.length ? new LinkedList(...tail) : null;
    }

    add(item) {
        if (this.tail) {
            this.tail.add(item);
        } else {
            this.tail = new LinkedList(item);
        }
    }

    has(item) {
        if (this.head === item) {
            return true;
        }
        if (this.tail === null) {
            return null;
        }
        return this.head.has(item);
    }
}
// tests

(0, _ava2.default)('LinkedList', t => {
    let list = new LinkedList(1, 2, 3, 8, 0, 44);
    list.add(4);
    list.add(5);
    t.is(list.has(1), true);
    t.is(list.has(4), true);
    t.is(list.has(6), false);
});
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/1030727ec4825f6d67f158ee8ce1b9c5.js.map