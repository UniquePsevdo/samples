'use strict';

var _ava = require('ava');

function permute(arr) {
    return permuteArr(arr.split(''));
}

function permuteArr(arr) {
    switch (arr.length) {
        case 0:
            return [];
        case 1:
            return arr;
        default:
            return flatten(arr.map(a => permuteArr(without(arr, a)).map(b => a.concat(b))));
    }
}

function without(arr, a) {
    let result = [...arr];
    result.splice(a.indexOf(a), 1);
    return result;
}

function flatten(arr) {
    return arr.reduce((acc, curr) => acc.concat(curr), []);
}

// tests


(0, _ava.test)(t => t.deepEqual(permute(''), []));
(0, _ava.test)(t => t.deepEqual(permute('abc'), ['abc', 'acb', 'bac', 'bca', 'cab', 'cba']));
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/eccfa6b12cbeec6f56068da4d8b10c3d.js.map