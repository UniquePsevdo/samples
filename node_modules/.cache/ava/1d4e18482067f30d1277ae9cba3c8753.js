'use strict';

var _ava = require('ava');

/*let fibonacci = function(){
    let cache =[0, 1];
    let fib = function(index){
        let result = cache[index]
        if(typeof result!=='number'){
            result = fib(index-1) + fib(index-2)
        }
        return result;
    }
    return fib;
}();*/

let fibonacci = function (idx) {
    let cache = [0, 1];
    let fib = function () {
        let result;
        if (typeof cache[idx] !== 'number') {
            result = fibonacci(idx);
            cache[idx] = result;
        }
        return result;
    };
    return fib;
}();

(0, _ava.test)(t => t.is(fibonacci(5), 5));
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/1d4e18482067f30d1277ae9cba3c8753.js.map