'use strict';

var _ava = require('ava');

/*
function uniq(arr){
    let seen = {}
    return arr.reduce((result, current)=>{
        if(!seen[current]){
            seen[current] = true
            result.push(current)
        }
        return result
    },[])
}
*/

function uniq(arr) {
    let seen = {};
    return arr.reduce((acc, curr) => {
        if (!seen[curr]) {
            acc.push(curr);
            seen[curr] = true;
        }
        return acc;
    }, []);
}

(0, _ava.test)(t => t.deepEqual(uniq([1, 4, 2, 2, 3, 4, 8]), [1, 4, 2, 3, 8]));
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/909fa48fc8a40beb1b322de6cf2503dc.js.map