'use strict';

/*let fibonacci = function(){
    let cache = [0, 1];
    let fib = function(n){
        let result = cache[n];
        if(typeof result !== 'number'){
            result = fib(n-1) + fib(n-2);
        }
        return result;
    }
    return fib;
}();*/

let fibonacci = function () {
    let cache = [0, 1];
    let fib = function (index) {
        let result = cache[index];
        if (typeof result !== 'number') {
            result = fib(index - 1) + fib(index - 2);
        }
        return result;
    };
    return fib;
}();

console.log(fibonacci(5));
//# sourceMappingURL=/Users/mac/WebstormProjects/interview/node_modules/.cache/ava/dfb5da881c37ae06848b9fd5d3b20001.js.map